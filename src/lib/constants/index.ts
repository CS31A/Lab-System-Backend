/**
 * @fileoverview Constants module for Lab System Backend API.
 * Contains application-wide constants used across the system.
 */

/**
 * Maximum allowed page size for pagination
 * @type {number}
 */
export const MAX_PAGE_SIZE = 100

/**
 * Default page size used when no page size is specified
 * @type {number}
 */
export const DEFAULT_PAGE_SIZE = 100
